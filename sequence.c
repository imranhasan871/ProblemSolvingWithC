#include <stdio.h>

int main(){

    int N,i,a,b;
            for (i = 1; i = a > b || a < b ;i++){

                scanf("%d %d",&a,&b);

                if (a == b)
                {
                    break;
                }
            else if (a < b)
                {
                    printf("Crescente\n");
                }
                else if(a >  b)
                {
                    printf("Decrescente");
                }
            }
     return 0;
}